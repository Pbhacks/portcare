import 'package:flutter/material.dart';

class PatientRecordsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Patient Records')),
      body: Center(child: Text('Patient records will be displayed here.')),
    );
  }
}
